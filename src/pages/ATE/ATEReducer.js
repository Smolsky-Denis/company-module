

const InitialState = {

};

function ateInfoReducer(state = InitialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

const ATEInfoReducer = {
    ate: ateInfoReducer
};

export default ATEInfoReducer;
